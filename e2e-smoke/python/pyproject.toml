[project]
name = "flow-healer-python-smoke"
version = "0.1.0"
description = "Python smoke fixture covering the add regression path"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
